@model ICollection<ParametroViewModel>

<table class="table table-striped table-bordered datatable">
    <colgroup>
        <col width="150" />
        <col />
        <col width="150" />
        <col width="250" />
        <col width="120" />
    </colgroup>
    <thead>
        <tr>
            <th>Nome</th>
            <th>Descrição</th>
            <th>Tipo</th>
            <th>Valor</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nome</td>
                <td>@item.Descricao</td>
                <td>@item.TipoNome</td>
                <td>@item.Valor</td>
                <td class="text-center">
                    <a class="btn btn-xs btn-default" asp-action="visualizar" asp-route-id="@item.Id">
                        <i class="fa fa-fw fa-search-plus"></i>
                    </a>
                    @if (Html.TemPermissao(FuncionalidadeEnum.Parametro, AcaoEnum.Editar))
                    {
                        <a class="btn btn-xs btn-default" asp-action="editar" asp-route-id="@item.Id">
                            <i class="fa fa-fw fa-edit"></i>
                        </a>
                    }
                    @if (Html.TemPermissao(FuncionalidadeEnum.Parametro, AcaoEnum.Excluir))
                    {
                        <a class="btn btn-xs btn-default" asp-action="excluir" asp-route-id="@item.Id" data-fw-delete-item>
                            <i class="fa fa-fw fa-trash-o"></i>
                        </a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>